package ObserverPattern;

/*
 * DisplayElement serves as an interface to all displays
 * 
 * displays() will be used when we need to call the displays 
 */
public interface DisplayElement {
	
	public void display();
}
